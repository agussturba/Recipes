management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: "jdbc:sqlserver://localhost:1433;databaseName=Recipes"
    username: agustin
    password:
